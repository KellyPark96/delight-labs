import { css } from "@emotion/react";

const Payment = () => {
  return (
    <div
      css={css`
        flex: 1;
        padding: 44px 28px 0;
      `}
    >
      payment
    </div>
  );
};

export default Payment;
