import { css } from "@emotion/react";

const Home = () => {
  return (
    <div
      css={css`
        flex: 1;
        padding: 44px 28px 0;
      `}
    >
      home
    </div>
  );
};

export default Home;
